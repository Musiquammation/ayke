Reduce this file as follows:

* Remove all drawing-related code.
* Remove `ClientData` and `Camera`.
* Remove `generateClientDom`.
* Remove `save`, `load`, `getSize`, `evalMouseCoords`, `getMobileDesc`, `createTutorial`, and `produceFinish`.
* Keep `createClient`, `createServer`, `runInput`, and `collectInputs`.
